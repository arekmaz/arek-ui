import { TagsCombobox } from "~/components/ui/tags-combobox";
import { Story } from "./storyHelpers";

export const TagsComboboxes = () => {
  return (
    <Story title="TAGS COMBOBOXES">
      <TagsCombobox />
    </Story>
  );
};
